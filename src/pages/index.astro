---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { Github, Mail, Zap, FileText, Rocket, Code, BookOpen, Target, ExternalLink, PenTool, Lightbulb } from "lucide-astro";
import { getContentStats } from "../utils/content";

// Get minimal data for initial load
const blogPosts = await getCollection("blog");
const projects = await getCollection("projects");
const contentStats = await getContentStats();

// Get only the most recent items for hero section
const recentBlogPosts = blogPosts
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime())
  .slice(0, 3);

const recentProjects = projects
  .filter(p => p.data.featured)
  .slice(0, 2);

// Current focus - most recent activity
const currentActivity = recentBlogPosts[0];
---

<BaseLayout title="Home">
  <div class="fullscreen-layout">
    <!-- Hero Section -->
    <section class="hero-section">
      <div class="integrated-header">
        <div class="header-content">
          <!-- Left: Profile Info -->
          <div class="profile-section">
            <img src="/avatar.jpg" alt="Jing Li Avatar" class="avatar" />
            <div class="profile-info">
              <h1>Jing Li</h1>
              <p class="bio">Building, learning, and sharing through code</p>
            </div>
          </div>
          
          <!-- Right: Stats and Actions -->
          <div class="header-actions">
            <div class="stats-row">
              <div class="stat">
                <span class="number">{contentStats.blogPosts}</span>
                <span class="label">Posts</span>
              </div>
              <div class="stat">
                <span class="number">{contentStats.projects}</span>
                <span class="label">Projects</span>
              </div>
              <div class="stat">
                <span class="number">{contentStats.snippets}</span>
                <span class="label">Snippets</span>
              </div>
            </div>
            <div class="action-buttons">
              <a href="https://github.com/jlog-dev" target="_blank" class="action-btn">
                <Github size={16} /> GitHub
              </a>
              <a href="mailto:you@example.com" class="action-btn">
                <Mail size={16} /> Email
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Current Focus -->
      {currentActivity && (
        <div class="current-focus" style="margin-top: var(--spacing-3xl);">
          <div class="focus-card">
            <div class="focus-header">
              <div class="status-indicator">
                <span class="pulse"></span>
                <span class="text">Latest</span>
              </div>
            </div>
            <h2><a href={`/blog/${currentActivity.slug}`}>{currentActivity.data.title}</a></h2>
            <p>{currentActivity.data.description}</p>
            <div class="focus-meta">
              <time>{currentActivity.data.pubDate.toLocaleDateString()}</time>
              <span class="type">Blog Post</span>
            </div>
          </div>
        </div>
      )}
    </section>

    <!-- Content Sections -->
    <section class="content-sections">
      <div class="content-wrapper">
        <div class="section-grid">
      <!-- Blog Section -->
      <div class="content-section">
        <div class="section-header">
          <h3><PenTool class="section-icon" /> Writing</h3>
          <a href="/blog" class="section-link">View all →</a>
        </div>
        <div class="section-preview">
          {recentBlogPosts.slice(0, 2).map((post) => (
            <article class="preview-item">
              <h4><a href={`/blog/${post.slug}`}>{post.data.title}</a></h4>
              <p>{post.data.description}</p>
              <time>{post.data.pubDate.toLocaleDateString()}</time>
            </article>
          ))}
        </div>
      </div>

      <!-- Projects Section -->
      <div class="content-section">
        <div class="section-header">
          <h3><Rocket class="section-icon" /> Building</h3>
          <a href="/projects" class="section-link">View all →</a>
        </div>
        <div class="section-preview">
          {recentProjects.map((project) => (
            <article class="preview-item">
              <h4><a href={`/projects/${project.slug}`}>{project.data.title}</a></h4>
              <p>{project.data.description}</p>
              <div class="project-status">{project.data.status}</div>
            </article>
          ))}
        </div>
      </div>

      <!-- Learning Section -->
      <div class="content-section">
        <div class="section-header">
          <h3><Lightbulb class="section-icon" /> Learning</h3>
          <a href="/til" class="section-link">View all →</a>
        </div>
        <div class="section-preview">
          <div class="learning-placeholder">
            <p>Daily discoveries and insights</p>
            <a href="/til" class="cta-button">Explore TIL →</a>
          </div>
        </div>
      </div>

      <!-- Tools Section -->
      <div class="content-section">
        <div class="section-header">
          <h3><Zap class="section-icon" /> Tools</h3>
          <a href="/snippets" class="section-link">View all →</a>
        </div>
        <div class="section-preview">
          <div class="tools-placeholder">
            <p>Code snippets and utilities</p>
            <a href="/snippets" class="cta-button">Browse snippets →</a>
          </div>
        </div>
      </div>
        </div>
      </div>
    </section>

    <!-- Activity Feed -->
    <section class="activity-section">
      <div class="activity-feed">
        <h2 class="section-title">
          <Zap class="section-icon" size={32} />
          Recent Activity
        </h2>
        <div class="activity-grid">
          <div class="activity-card">
            <div class="activity-icon">
              <FileText size={24} />
            </div>
            <div class="activity-info">
              <h4>Latest Post</h4>
              <p>Cyberpunk design patterns</p>
              <time>2 days ago</time>
            </div>
          </div>
          <div class="activity-card">
            <div class="activity-icon">
              <Rocket size={24} />
            </div>
            <div class="activity-info">
              <h4>Site Redesign</h4>
              <p>Full-screen layout complete</p>
              <time>1 week ago</time>
            </div>
          </div>
          <div class="activity-card">
            <div class="activity-icon">
              <Code size={24} />
            </div>
            <div class="activity-info">
              <h4>New Project</h4>
              <p>Component library released</p>
              <time>2 weeks ago</time>
            </div>
          </div>
          <div class="activity-card">
            <div class="activity-icon">
              <BookOpen size={24} />
            </div>
            <div class="activity-info">
              <h4>Learning</h4>
              <p>CSS animations mastered</p>
              <time>3 weeks ago</time>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>


  <style>
    /* Full-screen Layout */
    .fullscreen-layout {
      width: 100%;
      min-height: 100vh;
    }
    
    /* Section containers */
    .hero-section,
    .content-sections,
    .activity-section {
      min-height: 100vh;
      width: 100%;
      scroll-snap-align: start;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: center;
      padding: var(--spacing-2xl) var(--spacing-xl);
      padding-top: var(--spacing-3xl);
    }

    /* Cyberpunk Header */
    .hero-section .integrated-header {
      background: rgba(22, 33, 62, 0.9);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 2px solid var(--border-primary);
      border-radius: var(--radius-xl);
      margin: 0 0 var(--spacing-2xl) 0;
      padding: var(--spacing-xl) var(--spacing-2xl);
      box-shadow: var(--shadow-glow);
      position: relative;
      overflow: hidden;
      width: 100%;
      max-width: 1400px;
    }  

    .hero-section .integrated-header::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: var(--bg-gradient);
      box-shadow: 0 0 10px var(--color-neon-cyan);
    }
    
    .integrated-header::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        repeating-linear-gradient(
          90deg,
          transparent,
          transparent 2px,
          rgba(0, 245, 255, 0.03) 2px,
          rgba(0, 245, 255, 0.03) 4px
        );
      pointer-events: none;
      animation: scanlines 0.1s linear infinite;
    }
    
    @keyframes scanlines {
      0% { transform: translateX(0); }
      100% { transform: translateX(4px); }
    }

    .header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: var(--spacing-xl);
    }

    /* Profile Section */
    .profile-section {
      display: flex;
      align-items: center;
      gap: var(--spacing-lg);
    }

    .avatar {
      width: 80px;
      height: 80px;
      border-radius: var(--radius-full);
      border: 3px solid var(--border-primary);
      transition: all var(--transition-base);
      flex-shrink: 0;
      box-shadow: var(--shadow-glow);
      position: relative;
      overflow: hidden;
    }
    
    .avatar::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: conic-gradient(from 0deg, var(--color-neon-cyan), var(--color-neon-pink), var(--color-neon-yellow), var(--color-neon-cyan));
      animation: avatar-rotate 3s linear infinite;
      z-index: -1;
    }
    
    @keyframes avatar-rotate {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    .avatar:hover {
      transform: scale(1.1);
      box-shadow: var(--shadow-glow-pink);
      border-color: var(--color-neon-pink);
    }

    .profile-info h1 {
      font-size: var(--font-size-4xl);
      margin: 0 0 var(--spacing-xs) 0;
      color: var(--text-heading);
      font-weight: var(--font-weight-bold);
      font-family: var(--font-family-display);
      text-shadow: var(--shadow-neon-text);
      animation: neon-flicker 2s ease-in-out infinite alternate;
      letter-spacing: 0.05em;
      text-transform: uppercase;
    }
    
    @keyframes neon-flicker {
      0%, 100% { 
        color: var(--color-neon-cyan);
        text-shadow: 
          0 0 5px var(--color-neon-cyan),
          0 0 10px var(--color-neon-cyan),
          0 0 15px var(--color-neon-cyan),
          0 0 20px var(--color-neon-cyan);
      }
      50% { 
        color: var(--color-neon-pink);
        text-shadow: 
          0 0 5px var(--color-neon-pink),
          0 0 10px var(--color-neon-pink),
          0 0 15px var(--color-neon-pink),
          0 0 20px var(--color-neon-pink);
      }
    }

    .bio {
      font-size: var(--font-size-base);
      color: var(--text-secondary);
      margin: 0;
      line-height: var(--line-height-relaxed);
      font-family: var(--font-family-base);
      text-shadow: 0 0 3px rgba(160, 168, 204, 0.5);
    }

    /* Header Actions */
    .header-actions {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: var(--spacing-md);
    }

    .stats-row {
      display: flex;
      gap: var(--spacing-lg);
    }

    .stat {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--spacing-xs);
      text-align: center;
    }

    .stat .number {
      font-size: var(--font-size-xl);
      font-weight: var(--font-weight-bold);
      color: var(--color-accent);
      line-height: 1;
    }

    .stat .label {
      font-size: var(--font-size-xs);
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .contact-actions {
      display: flex;
      gap: var(--spacing-sm);
      align-items: center;
      gap: var(--spacing-xs);
      text-align: center;
    }

    .action-btn {
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
      padding: var(--spacing-md) var(--spacing-lg);
      background: var(--button-bg);
      border: 2px solid var(--border-primary);
      border-radius: var(--radius-xl);
      color: var(--button-text);
      text-decoration: none;
      font-size: var(--font-size-sm);
      font-weight: var(--font-weight-medium);
      font-family: var(--font-family-base);
      transition: all var(--transition-base);
      cursor: pointer;
      position: relative;
      overflow: hidden;
      backdrop-filter: blur(10px);
      box-shadow: var(--shadow-glow);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .action-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left var(--transition-slow);
    }

    .action-btn:hover {
      background: var(--button-hover-bg);
      color: var(--button-hover-text);
      border-color: var(--color-neon-pink);
      transform: translateY(-2px);
      box-shadow: var(--shadow-glow-pink);
      text-shadow: 0 0 5px var(--color-neon-pink);
    }

    .action-btn:hover::before {
      left: 100%;
    }

    .action-btn:focus {
      outline: none;
      box-shadow: var(--shadow-glow), 0 0 0 3px rgba(59, 130, 246, 0.1);
    }

    .action-btn svg {
      width: 16px;
      height: 16px;
    }

    .section-title {
      font-size: var(--font-size-2xl);
      font-weight: var(--font-weight-bold);
      color: var(--text-heading);
      margin: 0 0 var(--spacing-lg) 0;
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
      font-family: var(--font-family-display);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      text-shadow: 0 0 10px var(--color-neon-cyan);
    }

    .section-icon {
      width: 32px;
      height: 32px;
      filter: drop-shadow(0 0 10px var(--color-accent));
      animation: icon-glow 2s ease-in-out infinite alternate;
    }
    
    @keyframes icon-glow {
      0% { filter: drop-shadow(0 0 10px var(--color-accent)); }
      100% { filter: drop-shadow(0 0 20px var(--color-accent-hover)); }
    }

    /* Content Sections Container */
    .content-sections {
      background: 
        radial-gradient(circle at 20% 80%, rgba(0, 255, 65, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(255, 0, 255, 0.1) 0%, transparent 50%);
    }
    
    .content-wrapper {
      max-width: 1400px;
      width: 100%;
    }

    /* Hero Section */
    .hero-section {
      background: 
        radial-gradient(circle at 30% 40%, rgba(0, 245, 255, 0.15) 0%, transparent 50%),
        radial-gradient(circle at 70% 60%, rgba(255, 0, 255, 0.15) 0%, transparent 50%);
      position: relative;
    }
    

    /* Current Focus - Adjusted for main content */
    .current-focus {
      margin-bottom: var(--spacing-2xl);
      width: 100%;
      max-width: 1400px;
    }

    .focus-card {
      background: var(--bg-card);
      border: 1px solid var(--border-primary);
      border-radius: var(--radius-lg);
      padding: var(--spacing-xl);
      position: relative;
      overflow: hidden;
    }

    .focus-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--color-accent), var(--color-success));
    }

    .focus-header {
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
      margin-bottom: var(--spacing-md);
    }

    .status-indicator {
      display: flex;
      align-items: center;
      gap: var(--spacing-xs);
    }

    .pulse {
      width: 8px;
      height: 8px;
      background: var(--color-success);
      border-radius: 50%;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.7; transform: scale(1.1); }
    }

    .status-indicator .text {
      font-size: var(--font-size-sm);
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .focus-card h2 {
      margin: 0 0 var(--spacing-sm) 0;
      font-size: var(--font-size-xl);
      line-height: var(--line-height-tight);
    }

    .focus-card h2 a {
      color: var(--text-primary);
      text-decoration: none;
      transition: color var(--transition-base);
    }

    .focus-card h2 a:hover {
      color: var(--color-accent);
    }

    .focus-card p {
      color: var(--text-secondary);
      line-height: var(--line-height-relaxed);
      margin-bottom: var(--spacing-md);
    }

    .focus-meta {
      display: flex;
      gap: var(--spacing-md);
      align-items: center;
      font-size: var(--font-size-sm);
      color: var(--text-tertiary);
    }

    /* Content Sections Grid - Full Width */
    .content-sections {
      margin: var(--spacing-2xl) 0;
    }

    .section-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: var(--spacing-xl);
      max-width: 1400px;
      width: 100%;
    }

    .content-section {
      background: rgba(22, 33, 62, 0.8);
      border: 2px solid var(--border-primary);
      border-radius: var(--radius-xl);
      padding: var(--spacing-xl);
      transition: all var(--transition-base);
      position: relative;
      overflow: hidden;
      backdrop-filter: blur(15px);
      box-shadow: var(--shadow-glow);
    }

    .content-section:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-glow-pink);
      border-color: var(--color-neon-pink);
    }

    .content-section::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: var(--bg-gradient);
      transform: scaleX(0);
      transition: transform var(--transition-base);
      box-shadow: 0 0 10px var(--color-neon-cyan);
    }

    .content-section:hover::before {
      transform: scaleX(1);
    }

    .section-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--spacing-md);
      padding-bottom: var(--spacing-sm);
      border-bottom: 1px solid var(--border-primary);
    }

    .section-header h3 {
      margin: 0;
      font-size: var(--font-size-lg);
      color: var(--text-primary);
    }

    .section-link {
      color: var(--color-accent);
      text-decoration: none;
      font-size: var(--font-size-sm);
      font-weight: var(--font-weight-medium);
      transition: opacity var(--transition-base);
    }

    .section-link:hover {
      opacity: 0.8;
    }

    .section-preview {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-md);
    }

    .preview-item {
      padding: var(--spacing-md) 0;
      border-bottom: 1px solid var(--border-secondary);
    }

    .preview-item:last-child {
      border-bottom: none;
      padding-bottom: 0;
    }

    .preview-item h4 {
      margin: 0 0 var(--spacing-xs) 0;
      font-size: var(--font-size-base);
      line-height: var(--line-height-tight);
    }

    .preview-item h4 a {
      color: var(--text-primary);
      text-decoration: none;
      transition: color var(--transition-base);
    }

    .preview-item h4 a:hover {
      color: var(--color-accent);
    }

    .preview-item p {
      margin: 0 0 var(--spacing-xs) 0;
      color: var(--text-secondary);
      font-size: var(--font-size-sm);
      line-height: var(--line-height-relaxed);
    }

    .preview-item time {
      font-size: var(--font-size-xs);
      color: var(--text-tertiary);
    }

    .project-status {
      display: inline-block;
      background: var(--color-success);
      color: white;
      padding: var(--spacing-xs) var(--spacing-sm);
      border-radius: var(--radius-sm);
      font-size: var(--font-size-xs);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-top: var(--spacing-xs);
    }

    /* Placeholder Content */
    .learning-placeholder,
    .tools-placeholder {
      text-align: center;
      padding: var(--spacing-lg) 0;
    }

    .learning-placeholder p,
    .tools-placeholder p {
      color: var(--text-secondary);
      margin-bottom: var(--spacing-md);
    }

    .cta-button {
      display: inline-block;
      background: var(--color-accent);
      color: white;
      padding: var(--spacing-md) var(--spacing-xl);
      border-radius: var(--radius-xl);
      font-size: var(--font-size-sm);
      font-weight: var(--font-weight-medium);
      cursor: pointer;
      transition: all var(--transition-base);
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
      position: relative;
      overflow: hidden;
      box-shadow: var(--shadow-md);
    }

    .cta-button:hover {
      background: var(--color-accent-hover, var(--color-accent));
      opacity: 0.9;
    }

    /* Activity Section */
    .activity-section {
      background: 
        radial-gradient(circle at 50% 20%, rgba(0, 245, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 30% 80%, rgba(255, 0, 255, 0.1) 0%, transparent 50%);
    }
    
    /* Activity Feed - Progressive Loading */
    .activity-feed {
      max-width: 1400px;
      width: 100%;
    }

    .load-timeline-btn {
      background: var(--bg-gradient);
      color: var(--text-primary);
      border: 2px solid var(--border-primary);
      padding: var(--spacing-md) var(--spacing-xl);
      border-radius: var(--radius-xl);
      font-size: var(--font-size-sm);
      font-weight: var(--font-weight-medium);
      font-family: var(--font-family-base);
      cursor: pointer;
      transition: all var(--transition-base);
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
      position: relative;
      overflow: hidden;
      box-shadow: var(--shadow-glow);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      text-shadow: 0 0 5px var(--color-neon-cyan);
    }

    .load-timeline-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
      transition: left var(--transition-slow);
    }

    .load-timeline-btn:hover {
      transform: translateY(-2px) scale(1.02);
      box-shadow: var(--shadow-xl);
    }

    .load-timeline-btn:hover::before {
      left: 100%;
    }

    .load-timeline-btn:active {
      transform: translateY(0) scale(0.98);
    }

    .loading-spinner {
      animation: spin 1s linear infinite;
    }

    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    .feed-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--spacing-lg);
    }

    .feed-header h3 {
      margin: 0;
      font-size: var(--font-size-xl);
      color: var(--text-primary);
    }

    .load-more {
      background: var(--bg-secondary);
      border: 1px solid var(--border-primary);
      color: var(--text-primary);
      padding: var(--spacing-sm) var(--spacing-md);
      border-radius: var(--radius-md);
      cursor: pointer;
      font-size: var(--font-size-sm);
      transition: all var(--transition-base);
    }

    .load-more:hover {
      background: var(--color-accent);
      color: white;
      border-color: var(--color-accent);
    }

    .load-more:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    .feed-content {
      background: var(--bg-card);
      border: 1px solid var(--border-primary);
      border-radius: var(--radius-lg);
      padding: var(--spacing-lg);
    }

    .timeline-items {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-lg);
    }

    .timeline-item {
      display: flex;
      gap: var(--spacing-md);
      padding: var(--spacing-lg);
      border-radius: var(--radius-lg);
      transition: all var(--transition-base);
      position: relative;
      background: rgba(26, 26, 46, 0.8);
      border: 2px solid var(--border-primary);
      margin-bottom: var(--spacing-md);
      backdrop-filter: blur(10px);
      box-shadow: var(--shadow-sm);
    }

    .timeline-item:hover {
      background: rgba(22, 33, 62, 0.9);
      transform: translateX(8px);
      box-shadow: var(--shadow-glow);
      border-color: var(--color-neon-pink);
    }

    .timeline-item::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 4px;
      background: var(--bg-gradient);
      border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
      opacity: 0;
      transition: opacity var(--transition-base);
      box-shadow: 0 0 10px var(--color-neon-cyan);
    }

    .timeline-item:hover::before {
      opacity: 1;
    }

    .timeline-icon {
      width: 48px;
      height: 48px;
      border-radius: var(--radius-full);
      background: var(--bg-gradient);
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-primary);
      flex-shrink: 0;
      position: relative;
      overflow: hidden;
      border: 2px solid var(--border-primary);
      animation: timeline-icon-pulse 2s ease-in-out infinite;
    }

    .timeline-icon svg {
      width: 20px;
      height: 20px;
    }
    
    @keyframes timeline-icon-pulse {
      0%, 100% { box-shadow: var(--shadow-glow); }
      50% { box-shadow: var(--shadow-glow-pink); }
    }

    .timeline-icon::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(45deg, rgba(255, 255, 255, 0.1), transparent);
      border-radius: var(--radius-full);
    }

    .timeline-content h4 {
      margin: 0 0 var(--spacing-xs) 0;
      font-size: var(--font-size-base);
      color: var(--text-primary);
    }

    .timeline-content p {
      margin: 0 0 var(--spacing-xs) 0;
      color: var(--text-secondary);
      font-size: var(--font-size-sm);
    }

    .timeline-content time {
      font-size: var(--font-size-xs);
      color: var(--text-tertiary);
    }

    /* Activity Grid Styles */
    .activity-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: var(--spacing-lg);
      margin-bottom: var(--spacing-xl);
    }

    .activity-card {
      background: var(--bg-card);
      border: 1px solid var(--border-primary);
      border-radius: var(--radius-lg);
      padding: var(--spacing-lg);
      display: flex;
      align-items: center;
      gap: var(--spacing-md);
      transition: all var(--transition-base);
      position: relative;
      overflow: hidden;
    }

    .activity-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background: var(--bg-gradient);
      transform: scaleX(0);
      transform-origin: left;
      transition: transform var(--transition-base);
    }

    .activity-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-glow);
      border-color: var(--color-accent);
    }

    .activity-card:hover::before {
      transform: scaleX(1);
    }

    .activity-icon {
      width: 48px;
      height: 48px;
      border-radius: var(--radius-lg);
      background: var(--bg-gradient);
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--text-primary);
      flex-shrink: 0;
      border: 1px solid var(--border-primary);
    }

    .activity-info {
      flex: 1;
      min-width: 0;
    }

    .activity-info h4 {
      font-size: var(--font-size-base);
      font-weight: var(--font-weight-semibold);
      color: var(--text-heading);
      margin: 0 0 var(--spacing-xs) 0;
      line-height: 1.3;
    }

    .activity-info p {
      font-size: var(--font-size-sm);
      color: var(--text-secondary);
      margin: 0 0 var(--spacing-xs) 0;
      line-height: 1.4;
    }

    .activity-info time {
      font-size: var(--font-size-xs);
      color: var(--color-accent);
      font-family: var(--font-family-mono);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    /* Activity Summary */
    .activity-summary {
      background: var(--bg-card);
      border: 1px solid var(--border-primary);
      border-radius: var(--radius-lg);
      padding: var(--spacing-lg);
      text-align: center;
    }

    .summary-stats {
      display: flex;
      justify-content: center;
      gap: var(--spacing-xl);
      flex-wrap: wrap;
    }

    .stat-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--spacing-xs);
    }

    .stat-number {
      font-size: var(--font-size-2xl);
      font-weight: var(--font-weight-bold);
      color: var(--color-accent);
      font-family: var(--font-family-mono);
      text-shadow: 0 0 10px var(--color-accent);
    }

    .stat-label {
      font-size: var(--font-size-sm);
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
      .integrated-layout {
        max-width: 1200px;
      }
      
      .header-content {
        gap: var(--spacing-lg);
      }
      
      .stats-row {
        gap: var(--spacing-md);
      }
    }

    @media (max-width: 768px) {
      .integrated-layout {
        padding: 0 var(--spacing-sm);
      }
      
      .integrated-header {
        margin: var(--spacing-lg) 0;
        padding: var(--spacing-xl);
        backdrop-filter: blur(15px);
        -webkit-backdrop-filter: blur(15px);
      }
      
      .header-content {
        flex-direction: column;
        align-items: center;
        text-align: center;
        gap: var(--spacing-xl);
      }
      
      .profile-section {
        flex-direction: column;
        text-align: center;
        gap: var(--spacing-lg);
      }
      
      .profile-info h1 {
        font-size: var(--font-size-3xl);
      }
      
      .header-actions {
        align-items: center;
        width: 100%;
      }
      
      .stats-row {
        justify-content: center;
        flex-wrap: wrap;
        gap: var(--spacing-lg);
      }
      
      .contact-actions {
        justify-content: center;
        flex-wrap: wrap;
        gap: var(--spacing-md);
      }
      
      .action-btn {
        min-width: 120px;
        justify-content: center;
      }
      
      .section-grid {
        grid-template-columns: 1fr;
        gap: var(--spacing-xl);
      }
      
      .content-section {
        padding: var(--spacing-lg);
      }
      
      .section-title {
        font-size: var(--font-size-xl);
      }
      
      .feed-header {
        flex-direction: column;
        gap: var(--spacing-md);
        align-items: stretch;
      }
      
      .activity-grid {
        grid-template-columns: 1fr;
      }
      
      .activity-card {
        padding: var(--spacing-md);
      }
      
      .activity-icon {
        width: 40px;
        height: 40px;
      }

      .summary-stats {
        gap: var(--spacing-lg);
      }
    }

    @media (max-width: 480px) {
      .integrated-layout {
        padding: 0 var(--spacing-xs);
      }
      
      .integrated-header {
        padding: var(--spacing-lg);
        margin: var(--spacing-md) 0;
      }
      
      .avatar {
        width: 64px;
        height: 64px;
      }
      
      .profile-info h1 {
        font-size: var(--font-size-2xl);
      }
      
      .bio {
        font-size: var(--font-size-sm);
      }
      
      .stats-row {
        gap: var(--spacing-sm);
        justify-content: space-around;
      }
      
      .stat {
        min-width: 80px;
      }
      
      .stat .number {
        font-size: var(--font-size-lg);
      }
      
      .stat .label {
        font-size: 10px;
      }
      
      .contact-actions {
        flex-direction: column;
        width: 100%;
        gap: var(--spacing-sm);
      }
      
      .action-btn {
        width: 100%;
        justify-content: center;
        padding: var(--spacing-sm) var(--spacing-md);
      }
      
      .section-grid {
        gap: var(--spacing-lg);
      }
      
      .content-section {
        padding: var(--spacing-md);
      }
      
      .section-title {
        font-size: var(--font-size-lg);
      }
      
      .section-icon {
        width: 24px;
        height: 24px;
      }
    }
  </style>

  <activity-timeline></activity-timeline>
</BaseLayout>
